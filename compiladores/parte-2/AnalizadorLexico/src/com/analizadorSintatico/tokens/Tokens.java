package com.analizadorSintatico.tokens;

import java.util.HashMap;

public class Tokens {
    public static final HashMap<String, String> mapaToken = new HashMap<>();

    static {
        /* TIPOS NUMERICOS */
        mapaToken.put();
        mapaToken.put();

        /* TIPO ENDERECO */
        mapaToken.put();

        /* TIPO ALFANUMERICO */
        mapaToken.put();

        /* TIPOS COMENTARIOS */
        mapaToken.put();
        mapaToken.put();
        mapaToken.put();

        /* TIPOS DE SINAIS MATEMETICOS */
        mapaToken.put();
        mapaToken.put();

        /* TIPOS DE SINAIS LOGICOS */

        /* TIPO ATRIBUICAO */

        /* TIPOS DE TERMINACAO */

        /* TIPOS DE USO GERAL */
    }
}
